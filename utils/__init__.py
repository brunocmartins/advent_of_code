import os
import sys

# Add the repo root to Python path so utils can be imported from anywhere
_repo_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
if _repo_root not in sys.path:
    sys.path.insert(0, _repo_root)

